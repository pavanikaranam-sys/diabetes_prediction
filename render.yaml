services:
  - name: diabetes-app
    startCommand: streamlit run app/app.py --server.port 8000 --server.enableCORS false
    envVars:
      - key: PYTHONUNBUFFERED
        value: "true"
    autoDeploy: true

